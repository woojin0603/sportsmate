spring:
  datasource:
    url: jdbc:h2:file:./sportmap-local;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password: ''
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: false
app:
  mail:
    mode: ${EMAIL_MODE:mock}
  sms:
    mode: ${SMS_MODE:mock}
  auth:
    secure-cookie: false
  demo:
    enabled: false
server:
  address: 127.0.0.1
