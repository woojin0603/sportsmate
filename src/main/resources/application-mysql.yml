spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/sportmap?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul}
    username: ${DB_USER:sportmap}
    password: ${DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
